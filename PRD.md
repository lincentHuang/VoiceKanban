# VoiceKanban 卡片完成原地保留規範規格書 (PRD.md v7.0.0)

> **版本**：v7.0.0 (In-situ Card Completion Protocol - Preserve Column Placement)  
> **負責人**：`@PM`  
> **需求背景與決策**：
> 使用者明確提出：「卡片完成 不需要跑到 已完成欄位 就在原地就好」。
> 在各看板檢視（看板、表格、清單、收件匣、行事曆）中，點擊完成勾選框時，卡片應保持在當前所屬列表/欄位原地，不強制位移至「已完成 (done)」欄位，以維持使用者原本的工作流結構與空間記憶。

---

## 🎯 核心驗收標準 (Acceptance Criteria)

### 1. 單卡與批次原地完成 (AC-8.1)
- **[AC-8.1.1] toggleTaskComplete 原地切換**：點擊卡片右上角或詳細頁的「快速標記完成」按鈕，僅切換 `completed: true/false`，保留原本的 `columnId` 與 `boardId`，卡片不自動跳轉至 `done` 欄位。
- **[AC-8.1.2] batchToggleComplete 批次原地切換**：在多選模式下執行「批次完成」動作，選取的所有卡片狀態設為完成，各自保留原本所屬欄位（原列表、收件匣等），不進行欄位遷移。
- **[AC-8.1.3] 視覺回饋與狀態表現**：完成的卡片在原欄位內呈現刪除線、微透明底色（Opacity 65%）與綠色打勾標籤，再次點擊可取消完成狀態並恢復原狀。

### 2. 跨檢視與編輯彈窗一致性 (AC-8.2)
- **[AC-8.2.1] 編輯彈窗 EditTaskModal 儲存保護**：在編輯彈窗中修改標題、說明或標籤並儲存時，保留卡片原有的 `completed` 狀態，不因所在欄位非 `done` 而被強制重置為未完成。
- **[AC-8.2.2] 表格檢視 TableView 與清單檢視 ListView**：在表格與清單模式勾選完成框，卡片留在原列表列中，不自動變更所屬列表。
- **[AC-8.2.3] 側邊欄收件匣 SidebarInbox**：收件匣內的卡片被標記完成時，依然留在收件匣內原地呈現完成樣式。

### 3. 主動拖曳移動相容性 (AC-8.3)
- **[AC-8.3.1] 手動拖曳至「已完成」欄位**：若使用者主動將卡片拖曳或移動至 `done` 欄位，自動標記為 `completed: true`。
- **[AC-8.3.2] 手動拖曳離開「已完成」欄位**：若從 `done` 欄位主動拖曳至其他待辦/進行中欄位，自動解除完成狀態（`completed: false`）。
- **[AC-8.3.3] 完成卡片跨欄位移動**：已完成的卡片若在其他欄位間移動（例如從待辦移動至進行中），維持 `completed: true` 不變。
